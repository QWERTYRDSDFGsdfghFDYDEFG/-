# When the Sword Arrives — Codex 项目入口

> 适用范围：仓库根目录及全部子目录。进入更深目录工作时，还要读取当地的 `AGENTS.md`；若规则冲突，以距离目标文件最近的规则为准。

## Context

维护者同时负责本项目的策划、剧情改编、系统开发与演出设计。项目是一款基于《剑来》书简湖篇精神气质改编的非商业 Ren'Py 2D 叙事游戏 / 视觉小说原型，强调规矩、人情、选择、代价与问心，而非战力成长。当前阶段是持续打磨可玩原型，并将其推进到可交给外部测试者体验的 Demo。

## Goal

在不破坏现有 Ren'Py 工程、章节跳转和素材资产的前提下，完成一条稳定、连贯、可存读档的主线体验；逐步补齐剧情演出、角色与场景美术、音频氛围、选择反馈和 UI 可读性，使作品保持慢热、沉静、克制、压抑而有人情味的书简湖气质，并达到可公开试玩的质量。

## 开始工作前

1. 先读本文件。
2. 再读 `README.md`，确认仓库现状和项目入口。
3. 进入 `game/` 工作时，必须继续读 `game/AGENTS.md`。
4. 按 `game/AGENTS.md` 与任务类型补读 GDD、验收文档、相关脚本和资源规范；不要无目的加载整个仓库。
5. 检查当前工作树。已有修改默认属于用户，不覆盖、不回滚、不顺手整理无关内容。

## Constraints

- 技术栈以 Ren'Py / `.rpy` 为主；优先沿用现有结构、API、角色定义与资源组织，不为局部需求引入新的框架或重写工程。
- 先保证可启动、可推进、可存读档，再增强剧情、演出、美术、音频、UI 或系统表现。
- 不擅自删除、替换或大规模改写已有剧情、素材、系统文件和目录结构。
- 禁止递归或批量删除。确需删除时，只能处理用户明确授权的单个、已核对路径；批量删除交由用户执行。
- 不使用 `git reset --hard`、`git checkout --` 等方式丢弃用户改动，不修改与当前任务无关的脏文件。
- 新角色先在 `game/characters.rpy` 定义；新背景先在 `game/bg_images.rpy` 注册；配音通过 `game/audio_index.rpy` 的稳定 ID 引用。
- 剧情文本应拆成适合视觉小说阅读的短旁白和短对白，保持留白与克制，不擅自改变人物动机、核心剧情或问心主题。
- 角色图、场景图、CG、UI 和资源命名必须遵循 `game/AGENTS.md` 及 `game/Codex/` 中对应规范；没有用户明确要求时，新图以 1920×1080 横版为目标。
- 所有 API Key、secret 和凭证只放在根目录 `.env.local` 或用户指定的安全凭证系统中；不得写入源码、普通文本、日志、聊天回复或提交记录。
- `.env.local` 必须被 Git 忽略。需要示例时只创建不含真实值的 `.env.example`。
- 不打印、复述或提交凭证。若发现凭证已进入 Git 历史，立即停止传播并提醒用户去服务商处轮换；仅从最新提交删除不算修复。
- 未经用户明确授权，不发布、部署、推送、开 PR、上传素材或对外发送信息。
- 本项目为非商业同人原型。公开发布前，必须确认素材授权并替换不可公开或不可商用的音频、图片与参考资产。

## Conventions

- 任何新的功能构建、跨文件修改、重构或发布任务，先进入 Plan：说明目标理解、需要确认的问题、关键取舍、编号步骤和验证点；得到用户批准后再执行。
- 对单文件、低风险且用户已明确要求立即执行的修正，可以直接处理，但仍要先检查上下文与验证方式。
- 描述目标和验收结果，不自行扩大范围。信息不足且不同答案会实质改变结果时，先提问；能从仓库安全确认的内容自行调查。
- 修改前先定位引用关系，修改后按风险做最小充分验证。Ren'Py 任务至少检查语法、`label` / `jump`、角色定义及图片和音频路径。
- 能运行 Ren'Py 时执行 lint 或实际启动检查；不能运行时完成静态检查，并清楚列出未执行的验证。
- UI 或交互改动在交付前应做一次实际页面 / 游戏 QA，检查主流程、空状态、外链或跳转、可读性、常见分辨率和基础操作。
- 集成外部服务时，先做最小连接测试。失败后记录错误、已试方案和可复用结论；确认稳定的流程再考虑整理为项目 Skill。
- 每次生成、修改或确认角色图后，及时更新 `game/Codex/character_image_index.md`，登记主参考图、身份标准、用途、采用状态及关联分镜或演出表。
- 用户确认的脸部身份、年龄感、服装特征、画风结论、有效提示词和可复用的失败经验，应写入对应角色目录的标准文档；不要只保留在聊天记录中。
- 图片资产在索引中统一标记为“当前使用”“候选”“待修正”或“停用”。旧图不得因未采用而擅自删除，除非用户明确授权删除具体文件。
- `AGENTS.md` 只记录长期通用的出图规则，不逐张罗列图片，不复制完整提示词或临时生成过程；具体资产信息归入图片索引和角色标准文档。
- 只有当项目文件缺少已确认的母版、角色身份特征、最终提示词、否决原因或尚未保存的原图时，才定向回查相关聊天；不要无目的遍历全部历史聊天。
- 将会反复影响未来任务的经验写入最靠近适用范围的 `AGENTS.md` 或专门工作流文档；不要把临时故障、猜测和一次性细节堆进本文件。
- 保持改动小而聚焦，不留下临时文件、调试输出、死代码、无用目录或一次性草稿。
- 交付说明必须包含：修改文件、实现结果、验证结果、未验证项和已知风险。

## 常用验证入口

- Ren'Py 可执行文件位置记录在 `renpy-sdk-path.txt`；使用前先确认路径仍然有效。
- 第一版可玩验收以 `game/Codex/01_FIRST_PLAYABLE.md` 为准。
- 新游戏必须能从 `label start` 进入，并沿当前章节链推进到明确结束点。
- 最少检查主菜单、新游戏、章节跳转、选项、存档、读档、回看、快进、设置和退出。

## 文档维护

- `AGENTS.md` 只保留长期有效的项目上下文、目标、硬约束与协作约定。
- `README.md` 记录项目现状与入口；GDD 记录设计目标和叙事边界；Plan 记录某次任务的步骤；专项 SOP 放在 `game/Codex/`。
- 项目状态、路径或工作流发生变化时，同步更新唯一权威来源，避免在多份文档中复制出互相矛盾的版本。
